# v0.5.2: 自然语言 Agent 协作（规划草案）

## 目标

让 Agent 能够通过自然语言自主调用其他 Agent，实现真正的 Agent 协作。

---

## 当前限制

### v0.5.1 的 TODO
```rust
// registry.rs:103-111
// TODO: 实际执行 Agent
// 这里需要集成现有的 Agent 执行逻辑
// 暂时返回模拟结果
let agent_name = format!("{:?}", agent_type);
Ok(json!({
    "agent": agent_name,
    "input": input,
    "depth": ctx.depth(),
}))

// 实际实现应该是：
// let task = extract_task_from_input(&input)?;
// let output = execute_agent_sync(agent_type, &task)?;
```

### 问题
1. Agent Executor 返回模拟结果，不是真正的 Agent 调用
2. LLM 不知道可以调用其他 Agent
3. Agent 间无法传递结果

---

## 解决方案

### Phase 1: 集成真实 Agent 执行（2-3 小时）

**目标**：让 `AgentRegistry::call()` 调用真实的 Agent

#### 1.1 实现 Agent 调用适配器

```rust
// registry.rs
impl AgentRegistry {
    pub fn call(
        &self,
        agent_type: AgentType,
        input: Value,
        ctx: &mut CallContext,
    ) -> AgentCallResult {
        // ... 检查代码 ...

        // 🔥 真实执行 Agent
        let task = input["task"]
            .as_str()
            .or_else(|| input["prompt"].as_str())
            .unwrap_or("");

        let output = execute_agent_sync(agent_type, task)?;

        ctx.decrement_depth();
        Ok(serde_json::from_str(&output)?)
    }
}

// 调用现有的 Agent 执行逻辑
fn execute_agent_sync(agent_type: AgentType, task: &str) -> Result<String, AgentCallError> {
    match agent_type {
        AgentType::Explore => {
            let executor = ExploreAgentExecutor::new();
            executor.execute_sync(task)
        }
        AgentType::Review => {
            let executor = ReviewAgentExecutor::new();
            executor.execute_sync(task)
        }
        // ... 其他 Agent
    }
}
```

#### 1.2 为 AgentExecutor 添加 execute_sync 方法

```rust
// agentexecutors.rs
pub trait AgentExecutor {
    fn execute_sync(&self, task: &str) -> Result<String, ToolError>;
}

impl AgentExecutor for ExploreAgentExecutor {
    fn execute_sync(&self, task: &str) -> Result<String, ToolError> {
        // 调用现有的 execute 方法，但返回同步结果
        let result = self.execute(task.to_string(), None, &[])?;
        Ok(result)
    }
}
```

---

### Phase 2: LLM 工具调用集成（3-4 小时）

**目标**：让 LLM 知道可以调用其他 Agent

#### 2.1 添加 Agent 调用工具到工具定义

```rust
// tools.rs 或 create_tool_definitions()
pub fn create_agent_calling_tools() -> Vec<Tool> {
    vec![
        Tool {
            name: "call_agent".to_string(),
            description: r#"
调用其他 Agent 来完成子任务。

可用的 Agent:
- explore_agent: 探索和分析代码
- review_agent: 审查代码质量
- refactor_agent: 重构代码
- test_agent: 生成测试
- doc_agent: 生成文档
- debug_agent: 调试分析
- plan_agent: 任务规划
- react_agent: 深度推理
- git_commit_agent: 智能提交

参数:
- agent_type: Agent 类型（字符串）
- task: 任务描述（字符串）

示例:
- 需要探索代码 → call_agent("explore_agent", "分析用户认证模块")
- 需要审查代码 → call_agent("review_agent", "审查 auth.rs")
- 需要生成测试 → call_agent("test_agent", "为 Calculator 添加单元测试")
"#.to_string(),
            parameters: json!({
                "type": "object",
                "properties": {
                    "agent_type": {
                        "type": "string",
                        "enum": [
                            "explore_agent",
                            "review_agent",
                            "refactor_agent",
                            "test_agent",
                            "doc_agent",
                            "debug_agent",
                            "plan_agent",
                            "react_agent",
                            "git_commit_agent"
                        ]
                    },
                    "task": {
                        "type": "string",
                        "description": "要传递给 Agent 的任务描述"
                    }
                },
                "required": ["agent_type", "task"]
            }),
        },
    ]
}
```

#### 2.2 实现工具执行器

```rust
// new_tools/agent_call_tool.rs
#[derive(Tool)]
pub struct AgentCallTool;

impl ToolExecutor for AgentCallTool {
    fn execute(&self, args: Value, _ctx: &ExecutorContext) -> ToolResult {
        let agent_type_str = args["agent_type"].as_str().ok_or("missing agent_type")?;
        let task = args["task"].as_str().ok_or("missing task")?;

        // 解析 Agent 类型
        let agent_type = match agent_type_str {
            "explore_agent" => AgentType::Explore,
            "review_agent" => AgentType::Review,
            "refactor_agent" => AgentType::Refactor,
            // ...
            _ => return Err("unknown agent type".into()),
        };

        // 创建调用上下文
        let registry = AgentRegistry::global();
        let mut ctx = CallContext::new();

        // 调用 Agent
        let result = registry.call(agent_type, json!({"task": task}), &mut ctx)?;

        Ok(serde_json::to_string_pretty(&result)?)
    }
}
```

---

### Phase 3: Agent Prompt 更新（1-2 小时）

**目标**：让 Agent 知道可以调用其他 Agent

#### 3.1 更新 Agent Prompts

在每个 Agent 的 prompt 中添加：

```markdown
## 🔗 Agent 协作能力

你可以调用其他 Agent 来完成复杂任务：

### 何时调用其他 Agent

当你的任务需要：
- **探索代码** → 调用 `explore_agent`
- **审查代码** → 调用 `review_agent`
- **重构代码** → 调用 `refactor_agent`
- **生成测试** → 调用 `test_agent`
- **生成文档** → 调用 `doc_agent`
- **调试问题** → 调用 `debug_agent`
- **任务规划** → 调用 `plan_agent`
- **深度分析** → 调用 `react_agent`

### 如何调用

使用 `call_agent` 工具：
```
call_agent(
    agent_type = "review_agent",
    task = "审查 auth.rs 文件的安全问题"
)
```

### 协作示例

**场景 1: 需要探索后审查**
```
1. call_agent("explore_agent", "分析用户认证模块结构")
2. 基于探索结果，call_agent("review_agent", "审查发现的安全问题")
3. 综合两个 Agent 的结果，给出最终建议
```

**场景 2: 需要重构后测试**
```
1. call_agent("refactor_agent", "优化这个函数的性能")
2. call_agent("test_agent", "为重构后的代码添加测试")
3. 综合结果，确认重构成功
```

**重要**：
- 只在需要时调用其他 Agent
- 调用前确保你的主任务确实需要这个 Agent 的能力
- 调用后要整合结果，不要直接返回
```

---

### Phase 4: 端到端测试（1-2 小时）

#### 测试场景

```bash
# 测试 1: Agent 调用其他 Agent
ifai "
请帮我：
1. 探索项目结构
2. 审查认证模块
3. 识别安全问题
"

# 预期：Plan Agent 或 ReAct Agent 自动调用 Explore → Review

# 测试 2: 修复工作流
ifai "
审查代码并修复高严重性问题
"

# 预期：Review Agent → Plan Agent → Refactor Agent → Test Agent

# 测试 3: 完整 CI/CD
ifai "
帮我完成从分析到提交的完整流程
"

# 预期：Explore → Review → Refactor → Test → GitCommit
```

---

## 实施优先级

| Phase | 内容 | 预计时间 | 优先级 |
|-------|------|----------|--------|
| **Phase 1** | 集成真实 Agent 执行 | 2-3 小时 | 🔴 P0 |
| **Phase 2** | LLM 工具调用集成 | 3-4 小时 | 🔴 P0 |
| **Phase 3** | Agent Prompt 更新 | 1-2 小时 | 🟡 P1 |
| **Phase 4** | 端到端测试 | 1-2 小时 | 🟡 P1 |

**总预计时间**: 7-11 小时

---

## 成功标准

### 功能验证
- ✅ Agent 可以通过 `call_agent` 工具调用其他 Agent
- ✅ Agent 间结果能够正确传递
- ✅ 调用链能够正确追踪
- ✅ 权限检查正常工作

### 用户体验
- ✅ 用户可以一句话触发多 Agent 协作
- ✅ Agent 协作对用户透明
- ✅ 可以看到 Agent 调用链

### 测试覆盖
- ✅ 单元测试覆盖 Agent 互调用
- ✅ 端到端测试验证实际场景
- ✅ 708+ 测试继续通过

---

## 示例对话

### 用户请求
```
用户: 帮我优化用户认证模块的性能
```

### v0.5.1（当前）
```
Plan Agent: 我来分析这个问题...
[Plan Agent 自己分析，没有调用其他 Agent]
```

### v0.5.2（目标）
```
Plan Agent: 我来分解这个任务并协调其他 Agent

1. call_agent("explore_agent", "分析认证模块结构")
   → Explore Agent 返回：发现 3 个瓶颈函数

2. call_agent("react_agent", "深度分析性能瓶颈")
   → ReAct Agent 返回：login() 函数有不必要的循环

3. call_agent("refactor_agent", "优化 login() 函数")
   → Refactor Agent 返回：应用缓存策略

4. call_agent("test_agent", "验证性能改进")
   → Test Agent 返回：性能提升 60%

最终结果：✅ 性能优化完成，提升 60%
```

---

## 总结

**v0.5.1**: 实现了 Agent 协作的**基础设施**（代码层面）
**v0.5.2**: 实现 Agent 协作的**自然语言接口**（LLM 层面）

这就是"自然语言 Agent 协作"！🎯
