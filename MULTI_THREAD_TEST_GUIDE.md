# 多线程对话系统 - 手动测试指南

## 编译和运行

```bash
# 编译
cd /Users/mac/project/aieditor/ifainew/src-tauri
cargo build --release --bin ifai

# 运行
./target/release/ifai
```

## 测试场景

### 场景 1: 创建侧线程 (Ctrl+T)

**操作步骤**:
1. 启动 ifai
2. 在正常模式按下 `Ctrl+T`

**预期结果**:
- 状态栏显示: `» Side: Thread-1 (1/2) (Main) Esc ↩ to return`
- 线程图标: `»` (黄色)
- 线程名称: `Side: Thread-1`
- 线程索引: `(1/2)` - 表示第1个线程，总共2个
- 父线程: `(Main)` - 表示从主线程创建
- 操作提示: `Esc ↩ to return`

**验证点**:
- ✅ 状态栏显示正确
- ✅ 终端内容被清空
- ✅ 输入框仍然可用
- ✅ 线程数量增加到 2

---

### 场景 2: 创建多个侧线程

**操作步骤**:
1. 在主线程按 `Ctrl+T` (创建 Thread-1)
2. 在 Thread-1 按 `Ctrl+T` (创建 Thread-2)
3. 再按一次 `Ctrl+T` (创建 Thread-3)

**预期结果**:
- Thread-1 状态栏: `» Side: Thread-1 (2/4) (Main)`
- Thread-2 状态栏: `» Side: Thread-2 (3/4) (Side: Thread-1)`
- Thread-3 状态栏: `» Side: Thread-3 (4/4) (Side: Thread-2)`

**验证点**:
- ✅ 每个侧线程都有唯一的父线程
- ✅ 线程索引正确递增
- ✅ 最多支持 5 个线程

---

### 场景 3: 切换线程 (Alt+Left/Right)

**操作步骤**:
1. 创建 3 个线程 (Thread-1, Thread-2, Thread-3)
2. 在 Thread-3 按 `Alt+Left`

**预期结果**:
- 切换到 Thread-2
- 状态栏显示: `» Side: Thread-2 (3/4) (Side: Thread-1)`

**操作步骤**:
3. 再按 `Alt+Left` 两次

**预期结果**:
- 切换到 Main 线程
- 状态栏恢复正常模式（无 `»` 图标）

**操作步骤**:
4. 按 `Alt+Right`

**预期结果**:
- 切换到 Thread-1
- 状态栏显示: `» Side: Thread-1 (2/4) (Main)`

**验证点**:
- ✅ `Alt+Left` 切换到上一个线程
- ✅ `Alt+Right` 切换到下一个线程
- ✅ 线程切换后终端内容清空
- ✅ 主线程状态栏无 `»` 图标

---

### 场景 4: 返回父线程 (Esc)

**操作步骤**:
1. 在主线程按 `Ctrl+T` 创建 Thread-1
2. 在 Thread-1 按 `Ctrl+T` 创建 Thread-2
3. 在 Thread-2 按 `Esc`

**预期结果**:
- 切换回 Thread-1 (父线程)
- 状态栏显示: `» Side: Thread-1 (2/3) (Main)`
- `active_thread_mode` 保持 true

**操作步骤**:
4. 再按 `Esc`

**预期结果**:
- 切换回 Main 线程
- 状态栏恢复正常模式
- `active_thread_mode` 设为 false

**验证点**:
- ✅ `Esc` 返回父线程
- ✅ 主线程按 `Esc` 无效果（不退出线程模式外的其他模式）
- ✅ 到达主线程后自动退出线程模式

---

### 场景 5: 线程数量限制

**操作步骤**:
1. 连续按 `Ctrl+T` 6 次

**预期结果**:
- 前 5 次: 成功创建侧线程
- 第 6 次: 无反应（达到 5 个线程上限）

**验证点**:
- ✅ 最多支持 5 个线程 (1 Main + 4 Side)
- ✅ 超过限制时不创建新线程
- ✅ 不显示错误消息（当前未实现错误提示）

---

### 场景 6: 模式守卫测试

**操作步骤**:
1. 按 `Ctrl+O` 进入 Detail Overlay
2. 在 Overlay 模式下按 `Ctrl+T`

**预期结果**:
- 不创建新线程
- Overlay 保持激活

**操作步骤**:
3. 按 `Esc` 退出 Overlay
4. 按 `/test` (如果有 diff) 进入 Diff 模式
5. 在 Diff 模式下按 `Ctrl+T`

**预期结果**:
- 不创建新线程
- Diff 模式保持激活

**验证点**:
- ✅ Overlay 模式时不响应线程快捷键
- ✅ Diff 模式时不响应线程快捷键
- ✅ Search 模式时不响应线程快捷键
- ✅ 审批模式时不响应线程快捷键

---

## 快捷键总结

| 快捷键 | 功能 | 说明 |
|:-------|:-----|:-----|
| `Ctrl+T` | 创建侧线程 | 从当前线程创建子线程 |
| `Alt+Left` | 上一个线程 | 循环切换到上一个线程 |
| `Alt+Right` | 下一个线程 | 循环切换到下一个线程 |
| `Esc` | 返回父线程 | 在侧线程中返回父线程 |

## 状态栏显示格式

### 主线程
```
[Ready] Queue: 0 · Ctrl+O 查看详情
```

### 侧线程
```
» Side: Thread-1 (2/5) (Main) Esc ↩ to return
```

### 嵌套侧线程
```
» Side: Thread-2 (3/5) (Side: Thread-1) Esc ↩ to return
```

## 调试技巧

### 查看线程数量
状态栏中的 `(2/5)` 表示：
- `2`: 当前线程索引（从 1 开始）
- `5`: 线程总数

### 识别线程类型
- **主线程**: 状态栏无 `»` 图标
- **侧线程**: 状态栏有 `»` 图标

### 父线程信息
- `(Main)`: 从主线程创建
- `(Side: Thread-1)`: 从 Thread-1 创建

## 已知限制

1. **线程数量上限**: 5 个线程 (1 Main + 4 Side)
2. **消息路由**: 未实现（Phase 4）
3. **上下文继承**: 未实现（Phase 4）
4. **线程持久化**: 未实现（未来版本）
5. **错误提示**: 超过线程限制时无提示

## 测试检查清单

- [ ] 场景 1: 创建侧线程
- [ ] 场景 2: 创建多个侧线程
- [ ] 场景 3: 切换线程 (Alt+Left/Right)
- [ ] 场景 4: 返回父线程 (Esc)
- [ ] 场景 5: 线程数量限制
- [ ] 场景 6: 模式守卫测试

## 预期行为总结

✅ **已实现**:
- 线程创建和管理
- 线程切换 (Alt+Left/Right)
- 返回父线程 (Esc)
- 线程模式状态栏显示
- 模式守卫 (overlay/diff/search)

⏳ **未实现** (Phase 4+):
- AI 响应路由到对应线程
- 线程间消息隔离
- 上下文继承
- 线程持久化
