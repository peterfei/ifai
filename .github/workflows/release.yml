name: Release
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v0.2.6)'
        required: true
        type: string

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target aarch64-apple-darwin'
            target: 'aarch64-apple-darwin'
          - platform: 'macos-latest'
            args: '--target x86_64-apple-darwin'
            target: 'x86_64-apple-darwin'
          - platform: 'ubuntu-22.04'
            args: ''
            target: 'x86_64-unknown-linux-gnu'
          - platform: 'windows-latest'
            args: ''
            target: 'x86_64-pc-windows-msvc'

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev libwebkit2gtk-4.1-dev libsoup-3.0-dev libappindicator3-dev librsvg2-dev patchelf libssl-dev

      - name: Checkout Private Core
        uses: actions/checkout@v4
        with:
          repository: peterfei/ifainew-core # 您的私有核心库
          token: ${{ secrets.GH_PAT }}      # 必须在仓库 Secrets 中配置此 Token
          path: .ifainew-core-temp          # 先下载到当前工作区的临时目录

      - name: Restore Core Directory Structure
        shell: bash
        run: |
          # 将核心库移动到上级目录，以匹配本地开发的相对路径 (../ifainew-core)
          mv .ifainew-core-temp ../ifainew-core

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Set macOS deployment target (local LLM requires 10.15+)
        if: startsWith(matrix.platform, 'macos')
        shell: bash
        run: |
          echo "MACOSX_DEPLOYMENT_TARGET=10.15" >> $GITHUB_ENV

      - name: Install Core Frontend Dependencies
        run: |
          cd ../ifainew-core/typescript
          npm install

      - name: Install frontend dependencies
        shell: bash
        env:
          NODE_OPTIONS: '--max-old-space-size=8192'
        run: |
          rm -rf node_modules package-lock.json
          npm install --legacy-peer-deps

      - name: Increase Node.js memory limit for build
        shell: bash
        run: |
          echo "NODE_OPTIONS=--max-old-space-size=8192" >> $GITHUB_ENV
          echo "APP_EDITION=commercial" >> $GITHUB_ENV

      - name: Verify Tauri CLI
        run: |
          npx tauri --version || npm install @tauri-apps/cli@latest --force

      # Windows-specific: Create self-signed certificate for MSIX
      - name: Create self-signed certificate (Windows)
        if: matrix.platform == 'windows-latest'
        shell: powershell
        run: |
          $cert = New-SelfSignedCertificate -Type Custom -Subject "CN=BFB76822-8992-4D24-B38C-71F0C2BABD49" -KeyUsage DigitalSignature -FriendlyName "IfAI Self Signed" -CertStoreLocation "Cert:\CurrentUser\My" -TextExtension @("2.5.29.37={text}1.3.6.1.5.5.7.3.3", "2.5.29.19={text}")
          $password = ConvertTo-SecureString -String "Password123!" -Force -AsPlainText
          Export-PfxCertificate -Cert $cert -FilePath "my-cert.pfx" -Password $password
          echo "TAURI_SIGNING_PRIVATE_KEY=$(pwd)\my-cert.pfx" >> $env:GITHUB_ENV
          echo "TAURI_SIGNING_PRIVATE_KEY_PASSWORD=Password123!" >> $env:GITHUB_ENV

      - name: Build the app
        uses: tauri-apps/tauri-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NODE_OPTIONS: '--max-old-space-size=8192'
          # Windows signing env vars
          TAURI_CERT_PATH: ${{ env.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_CERT_PASSWORD: ${{ env.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'IfAI v__VERSION__'
          releaseBody: 'See the assets to download this version and install.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }} --features commercial-local-llm
          beforeBuildCommand: 'npm run build:commercial'
