name: Windows Test Build

on:
  push:
    branches:
      - 'hotfix/**'  # 推送到 hotfix 前缀的分支时自动触发
  workflow_dispatch:  # 同时保留手动触发

jobs:
  windows-only:
    permissions:
      contents: write
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout Private Core
        uses: actions/checkout@v4
        with:
          repository: peterfei/ifainew-core
          token: ${{ secrets.GH_PAT }}
          path: .ifainew-core-temp

      - name: Restore Core Directory Structure
        shell: bash
        run: |
          mv .ifainew-core-temp ../ifainew-core

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Install Core Frontend Dependencies
        shell: bash
        run: |
          cd ../ifainew-core/typescript
          npm install

      - name: Install Frontend Dependencies
        run: npm install

      - name: Build Frontend
        run: npm run build

      - name: Extract Branch Name
        id: branch_name
        shell: bash
        run: |
          # 提取分支名作为测试标识 (如 hotfix/windows-flicker-phase1 -> windows-flicker-phase1)
          BRANCH="${GITHUB_REF#refs/heads/}"
          TEST_NAME="${BRANCH#hotfix/}"
          echo "test_name=$TEST_NAME" >> $GITHUB_OUTPUT
          echo "branch=$BRANCH" >> $GITHUB_OUTPUT

      - name: Build Windows Only
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: --features commercial-local-llm
          beforeBuildCommand: 'npm run build:commercial'
          includeDebug: false

      - name: Upload as Draft Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: test-windows-${{ steps.branch_name.outputs.test_name }}-${{ github.run_number }}
          name: 🧪 Windows Test - ${{ steps.branch_name.outputs.test_name }} #${{ github.run_number }}
          body: |
            ## Windows 测试构建

            - **分支**: `${{ steps.branch_name.outputs.branch }}`
            - **提交**: `${{ github.sha }}`
            - **构建时间**: `${{ github.event.head_commit.timestamp }}`

            ---


            ⚠️ **这是测试构建版本，仅供内部测试使用，不代表正式发布。**


            ## 下载说明

            | 文件 | 说明 |
            |------|------|
            | `IfAI_{version}_x64-setup.exe` | NSIS 安装包（推荐） |
            | `IfAI_{version}_x64_en-US.msi` | MSI 安装包 |

            ## 测试重点

            - [ ] Windows 下流式代码生成无闪屏
            - [ ] Agent 工具调用流畅
            - [ ] 多线程切换正常
            - [ ] UI 渲染无异常

            ---
            *由 GitHub Actions 自动生成*
          draft: true
          prerelease: true
          files: |
            src-tauri/target/release/bundle/nsis/IfAI_*.exe
            src-tauri/target/release/bundle/msi/IfAI_*.msi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
