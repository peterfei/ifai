// Generated by generate_tests.rs
// DO NOT EDIT MANUALLY
// Source: /Users/mac/project/aieditor/ifainew/src-tauri/src/bin/ifai/tests/suite/session_compression.yaml
// Suite: 会话压缩测试

use crate::tests::common::*;

#[tokio::test]
#[serial_test::serial]
async fn test_ya_suochu_fa() {
    // P0 - 验证会话超过 100k tokens 时自动触发压缩
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_stdin("start a long conversation\ncontinue\nkeep going\nmore content\nstill talking\nand more\neven more\nlots of tokens\nalmost there\ntrigger compression now\n/exit");
    // Mock streaming response
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello", " World"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_compression_triggered();
}

#[tokio::test]
#[serial_test::serial]
async fn test_ya_suobao_liushang_xia_wen() {
    // P0 - 验证压缩后保留重要的系统提示词和最近消息
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_stdin("my favorite color is blue\nremember that\nwhat is my favorite color\n/exit");
    // Mock streaming response
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello", " World"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_system_prompt_preserved();
    output.assert_recent_messages_count(5);
}

#[tokio::test]
#[serial_test::serial]
async fn test_ya_suo_dui_hua() {
    // P0 - 验证压缩后能正常继续对话
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_stdin("let's discuss rust\ntell me about ownership\nwhat about borrowing\nexplain lifetimes\nmore details\nkeep explaining\ncompression should happen\ncontinue with traits\n/exit");
    // Mock streaming response
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello", " World"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_compression_triggered();
}

#[tokio::test]
#[serial_test::serial]
async fn test_ya_suo_gong_judiao_yong() {
    // P0 - 验证压缩能正确处理包含工具调用的会话
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_stdin("list files here\nshow main.rs\ncount lines\ncheck git status\nmore operations\nkeep using tools\ntrigger compression\n/exit");
    // Mock streaming response
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello", " World"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_compression_triggered();
}

#[tokio::test]
#[serial_test::serial]
async fn test_ya_suo_token() {
    // P1 - 验证压缩后 Token 计数正确更新
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_stdin("start conversation\nadd more context\neven more\nlots of tokens\ntrigger compression\n/status\n/exit");
    // Mock streaming response
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello", " World"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_token_count_below(20000);
}

#[tokio::test]
#[serial_test::serial]
async fn test_ya_suo() {
    // P1 - 验证压缩使用摘要策略保留关键信息
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_stdin("we're discussing rust ownership model\nownership means one owner\nmove semantics transfer ownership\ncopy trait for types with copy semantics\nclone creates deep copy\nreference counting with Rc\narc for thread safe\ntrigger compression\nwhat were we discussing\n/exit");
    // Mock streaming response
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello", " World"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_contains("ownership");
}

#[tokio::test]
#[serial_test::serial]
async fn test_chu_faya_suo() {
    // P1 - 验证 /compact 命令能手动触发压缩
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_stdin("start conversation\nadd some context\nmore messages\n/compact\ncontinue after compression\n/exit");
    // Mock streaming response
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello", " World"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_contains("compressed");
}

#[tokio::test]
#[serial_test::serial]
async fn test_ya_suobao_liuxi_tongti_shi_ci() {
    // P1 - 验证压缩后系统提示词完整保留
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_stdin("you are a rust expert\nexplain ownership\nexplain borrowing\nkeep explaining\ntrigger compression\nwhat is your role\n/exit");
    // Mock streaming response
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello", " World"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_system_prompt_preserved();
}

