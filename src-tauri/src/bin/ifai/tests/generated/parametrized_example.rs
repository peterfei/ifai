// Generated by build.rs
// DO NOT EDIT MANUALLY
// Original source: tests/suite
// Suite: 参数化测试示例

use crate::tests::common::*;

#[tokio::test]
#[serial_test::serial]
async fn test_test_2f554_provider_openai() {
    // 参数化测试:
    //   provider = openai
    // 验证不同 AI 提供商的连接
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "openai");
    env.set_env("IFAI_MODEL", "openai-model");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_contains("openai");
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2f554_provider_deepseek() {
    // 参数化测试:
    //   provider = deepseek
    // 验证不同 AI 提供商的连接
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "deepseek");
    env.set_env("IFAI_MODEL", "deepseek-model");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_contains("deepseek");
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2f554_provider_anthropic() {
    // 参数化测试:
    //   provider = anthropic
    // 验证不同 AI 提供商的连接
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "anthropic");
    env.set_env("IFAI_MODEL", "anthropic-model");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_contains("anthropic");
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2c828_model_gpt_4o_mini() {
    // 参数化测试:
    //   model = gpt-4o-mini
    // 验证不同模型的兼容性
    let mut env = TestEnv::with_mock().await.unwrap();
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["--model", "gpt-4o-mini", "hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2c828_model_gpt_4_turbo() {
    // 参数化测试:
    //   model = gpt-4-turbo
    // 验证不同模型的兼容性
    let mut env = TestEnv::with_mock().await.unwrap();
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["--model", "gpt-4-turbo", "hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2c828_model_claude_3_haiku() {
    // 参数化测试:
    //   model = claude-3-haiku
    // 验证不同模型的兼容性
    let mut env = TestEnv::with_mock().await.unwrap();
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["--model", "claude-3-haiku", "hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2c828_model_deepseek_chat() {
    // 参数化测试:
    //   model = deepseek-chat
    // 验证不同模型的兼容性
    let mut env = TestEnv::with_mock().await.unwrap();
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["--model", "deepseek-chat", "hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_3ead5_provider_openai_model_small() {
    // 参数化测试:
    //   provider = openai
    //   model = small
    // 验证提供商和模型的组合
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "openai");
    env.set_env("IFAI_MODEL", "small");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_3ead5_provider_openai_model_large() {
    // 参数化测试:
    //   provider = openai
    //   model = large
    // 验证提供商和模型的组合
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "openai");
    env.set_env("IFAI_MODEL", "large");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_3ead5_provider_deepseek_model_small() {
    // 参数化测试:
    //   provider = deepseek
    //   model = small
    // 验证提供商和模型的组合
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "deepseek");
    env.set_env("IFAI_MODEL", "small");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_3ead5_provider_deepseek_model_large() {
    // 参数化测试:
    //   provider = deepseek
    //   model = large
    // 验证提供商和模型的组合
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "deepseek");
    env.set_env("IFAI_MODEL", "large");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_pei_zhi_you_xian_ji_config_source_CLI_参数() {
    // 参数化测试:
    //   config_source = CLI 参数
    // 验证不同配置源的优先级
    let mut env = TestEnv::with_mock().await.unwrap();
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_pei_zhi_you_xian_ji_config_source_环境变量() {
    // 参数化测试:
    //   config_source = 环境变量
    // 验证不同配置源的优先级
    let mut env = TestEnv::with_mock().await.unwrap();
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_pei_zhi_you_xian_ji_config_source_配置文件() {
    // 参数化测试:
    //   config_source = 配置文件
    // 验证不同配置源的优先级
    let mut env = TestEnv::with_mock().await.unwrap();
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

