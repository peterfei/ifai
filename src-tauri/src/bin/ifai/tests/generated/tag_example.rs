// Generated by build.rs
// DO NOT EDIT MANUALLY
// Original source: tests/suite
// Suite: 标签测试示例

use crate::tests::common::*;

#[tokio::test]
#[serial_test::serial]
async fn test_ming_ling() {
    // tags: smoke, fast, unit
    // 验证基础命令可用
    let mut env = TestEnv::new().await.unwrap();
    let output = env.run_cli(&["--help"]).await.unwrap();
    output.assert_success();
    output.assert_contains("USAGE");
}

#[tokio::test]
#[serial_test::serial]
async fn test_ban_benxin_xi() {
    // tags: unit, fast
    // 验证版本命令
    let mut env = TestEnv::new().await.unwrap();
    let output = env.run_cli(&["--version"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_jian_dandui_hua() {
    // tags: integration, mock
    // 测试简单的 AI 对话
    let mut env = TestEnv::with_mock().await.unwrap();
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2f554_provider_openai() {
    // tags: integration, parametrized
    // 参数化测试:
    //   provider = openai
    // 验证不同 AI 提供商的连接
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "openai");
    env.set_env("IFAI_MODEL", "openai-model");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2f554_provider_deepseek() {
    // tags: integration, parametrized
    // 参数化测试:
    //   provider = deepseek
    // 验证不同 AI 提供商的连接
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "deepseek");
    env.set_env("IFAI_MODEL", "deepseek-model");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2f554_provider_anthropic() {
    // tags: integration, parametrized
    // 参数化测试:
    //   provider = anthropic
    // 验证不同 AI 提供商的连接
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "anthropic");
    env.set_env("IFAI_MODEL", "anthropic-model");
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_liu_shixiang_ying() {
    // tags: integration, slow, streaming
    // 测试流式 AI 响应
    let mut env = TestEnv::with_mock().await.unwrap();
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Response"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

