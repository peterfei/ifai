// Generated by generate_tests.rs
// DO NOT EDIT MANUALLY
// Source: /Users/mac/project/aieditor/ifainew/src-tauri/src/bin/ifai/tests/suite/config_precedence.yaml
// Suite: 配置优先级测试

use crate::tests::common::*;

#[tokio::test]
#[serial_test::serial]
async fn test_cli() {
    // 验证 CLI 参数优先级高于环境变量
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "openai");
    env.set_env("IFAI_MODEL", "gpt-4");
    // Mock response: simple_response.json
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello from mock!"]).await.unwrap();
    }
    let output = env.run_cli(&["--provider", "deepseek", "--model", "deepseek-chat"]).await.unwrap();
    output.assert_success();
    output.assert_contains("deepseek");
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_4776d() {
    // 验证环境变量优先级高于配置文件
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "openai");
    env.set_env("IFAI_MODEL", "gpt-4o-mini");
    env.write_config("provider = \"deepseek\"\nmodel = \"deepseek-chat\"\n[deepseek]\napi_key = \"sk-file-key\"\n").await.unwrap();
    // Mock response: simple_response.json
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello from mock!"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_contains("openai");
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_4404c() {
    // 验证配置文件优先级高于 YAML 默认值
    let mut env = TestEnv::with_mock().await.unwrap();
    env.write_config("provider = \"openai\"\nmodel = \"gpt-4o-mini\"\n[openai]\napi_key = \"sk-file-key\"\n").await.unwrap();
    // Mock response: simple_response.json
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello from mock!"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
    output.assert_contains("openai");
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_27505() {
    // 验证 CLI > Env > File > Default 完整链
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_PROVIDER", "openai");
    env.set_env("IFAI_MODEL", "gpt-4o-mini");
    env.write_config("provider = \"anthropic\"\nmodel = \"claude-3-haiku\"\n[anthropic]\napi_key = \"sk-file-key\"\n").await.unwrap();
    // Mock response: simple_response.json
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello from mock!"]).await.unwrap();
    }
    let output = env.run_cli(&["--provider", "deepseek", "hello"]).await.unwrap();
    output.assert_success();
    output.assert_contains("deepseek");
}

#[tokio::test]
#[serial_test::serial]
async fn test_test_2e159() {
    // 验证 --config init 生成正确的配置模板
    let mut env = TestEnv::new().await.unwrap();
    let output = env.run_cli(&["--config", "init"]).await.unwrap();
    output.assert_success();
    output.assert_match(r"provider.*model.*api_key");
}

#[tokio::test]
#[serial_test::serial]
async fn test_api_key_cli() {
    // 验证 API Key 的优先级（CLI > Env > File）
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("OPENAI_API_KEY", "sk-env-key");
    env.write_config("provider = \"openai\"\nmodel = \"gpt-4o-mini\"\n[openai]\napi_key = \"sk-file-key\"\n").await.unwrap();
    // Mock response: simple_response.json
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello from mock!"]).await.unwrap();
    }
    let output = env.run_cli(&["--api-key", "sk-cli-key", "hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_api_key_env() {
    // 验证环境变量 API Key 覆盖配置文件
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("OPENAI_API_KEY", "sk-env-key");
    env.write_config("provider = \"openai\"\nmodel = \"gpt-4o-mini\"\n[openai]\napi_key = \"sk-file-key\"\n").await.unwrap();
    // Mock response: simple_response.json
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello from mock!"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_base_url() {
    // 验证 Base URL 优先级（IFAI_API_BASE 环境变量）
    let mut env = TestEnv::with_mock().await.unwrap();
    env.set_env("IFAI_API_BASE", "https://custom.api.com/v1");
    // Mock response: simple_response.json
    if let Some(mock) = env.mock_server() {
        mock.setup_streaming_response(vec!["Hello from mock!"]).await.unwrap();
    }
    let output = env.run_cli(&["hello"]).await.unwrap();
    output.assert_success();
}

#[tokio::test]
#[serial_test::serial]
async fn test_xian_shi() {
    // 验证 --config show 正确显示当前配置
    let mut env = TestEnv::new().await.unwrap();
    env.set_env("IFAI_PROVIDER", "openai");
    env.set_env("IFAI_MODEL", "gpt-4o-mini");
    let output = env.run_cli(&["--config", "show"]).await.unwrap();
    output.assert_success();
    output.assert_match(r"provider.*openai");
}

